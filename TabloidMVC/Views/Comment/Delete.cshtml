@model TabloidMVC.Models.Comment

@{
    ViewData["Title"] = "Delete";
}

<div class="container pt-5">
    <h1>Delete</h1>

    <h3>Are you sure you want to delete this?</h3>
    <div>
        <table class="table table-striped">
            <thead>
                <tr>
                    <th> @Html.DisplayNameFor(model => model.Subject)</th>
                    <th> @Html.DisplayNameFor(model => model.Content)</th>
                    <th> @Html.DisplayNameFor(model => model.CreateDataTime)</th>
                </tr>
            <tbody>
                <tr>
                    <td> @Html.DisplayFor(model => model.Subject)</td>
                    <td>@Html.DisplayFor(model => model.Content)</td>
                    <td>@Html.DisplayFor(model => model.CreateDataTime)</td>
                </tr>
            </tbody>
        </table>
        <form asp-action="Delete">
            <input type="submit" value="Delete" class="btn btn-danger" />
            <a asp-action="Index" asp-route-id="@Model.PostId" class="btn btn-primary" title="Back To List">Back To List</a>
        </form>
    </div>
</div>